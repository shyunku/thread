# Task 49: Vault·멤버십·기기 권한 API

생성: 2026-09-08 12:35 (KST)

최근 진행(2026-09-09 11:07 KST): [구현·검증·남은 범위](../reports/2026-09-09-e2ee-checkpoint.md). 현재 앱의 E2EE 활성화/운영 이관을 완료한 것은 아니며 WIP를 유지한다.

상태: 🟢 DONE

완료: 2026-09-09 13:38 (KST). 추가 schema 및 genesis/승인/해지/복구 권한 API의 자동 완료 조건 충족. active 회전·수신자 envelope·권한·멱등성·만료·격리 MySQL 회귀 및 전체 Go 테스트 통과. [현재 API 계약·검증](../protocol/e2ee-v3-api.md). E2EE_API_ENABLED 기본 false; 운영 활성화/사용자 UI/#47 외부 검토/#57 출시는 완료를 뜻하지 않는다.

업데이트: 2026-09-09 10:04 (KST)

착수: 추가 전용 schema 및 인증 principal 경계부터 구현. #47 미완료 상태에서는 서명 검증 없는 쓰기 API를 노출하지 않는다. 기존 계정 전환 및 운영 DB 적용은 하지 않는다.

선행: #47

설계: [E2EE 패치](../patches/patch-20260907-e2ee.md)

범위: 새 migration 버전으로 vault/genesis/devices/key-envelope 추가. user principal 분리, 관리자 우회 금지, CAS·rollback/fork 한계, readonly 기기 쓰기 거부. 기존 평문 계정 자동 전환 금지.

완료 조건: 추가 schema migration 및 승인·해지·복구 권한 검증.

## 2026-09-09 09:55 (KST)

- migration 6에 vaults/events/devices/envelopes 추가. 과거 migration SQL 및 기존 데이터는 변경하지 않는다. 다음 서버 시작 시 빈 테이블 추가는 실행되지만 계정 E2EE 활성화/이관은 실행되지 않는다.
- v3 사용자 인증 middleware 구현. 관리자/역할 누락·오형식/예약 관리자 subject/만료·알고리즘 오류 차단, body accountId 무시. 아직 라우터에 연결하지 않았다.
- [서버 기반 명세](../protocol/e2ee-server-foundation.md)에 저장 제약과 미완료 서명/CAS 경계를 기록했다.
- Go 인증 및 migration 단위 테스트 통과. MySQL 전용 테스트는 Docker engine 부재로 미실행. 재실행을 위한 disposable DB 제약 테스트를 추가했다.
- 승인/해지/복구/CAS 및 서명 검증 API는 미완료. #49 WIP 유지. 운영 DB·env 읽기·서버 시작·배포는 수행하지 않았다.

## MySQL 실검증 — 2026-09-09 10:04 (KST)

- 사용자가 Docker 시작 후 독립 mysql:8.0 컨테이너의 tmpfs DB(thread_migration_test_e2ee)에서 실제 migration lifecycle 및 v3 인증 테스트 통과. 기존 Compose/볼륨/계정 데이터는 사용하지 않았다.
- 기존 schema 2→6 추가, 반복 실행, 기존 synthetic 평문 보존, 동시 migration 1회 적용, downgrade/checksum drift/부분 DDL 실패 재실행 차단 검증 통과.
- 계정당 vault uniqueness, revision 중복, 미승인 revision 참조, 잘못된 role, 없는 수신자 및 envelope 덮어쓰기 거부 검증 통과.
- 서명된 승인/해지/복구 API 검증을 대신하지 않는다. #49 WIP 유지. 테스트 컨테이너는 검증 후 중지/자동 제거하고 tmpfs fixture 데이터만 폐기한다.
