# Task 48: 암호화 로컬 DB·키 보관·잠금

생성: 2026-09-08 12:35 (KST)

최근 진행(2026-09-09 11:07 KST): [구현·검증·남은 범위](../reports/2026-09-09-e2ee-checkpoint.md). 현재 앱의 E2EE 활성화/운영 이관을 완료한 것은 아니며 WIP를 유지한다.

상태: 🟡 WIP

2026-09-09 19:32 (KST): 사용자 실제 개발 앱 검증으로 비밀번호 및 OS 인증 잠금 해제 정상 확인. 방식 선택 버튼은 인증 실행 버튼과 별개이며 최종 UI에서 통합·정리할 예정. 재실행/인증 취소는 이번 확인에 포함하지 않는다. 개발 보관함 런타임 폴더는 Git 제외 대상으로 보강했다.

2026-09-09 18:59 (KST): 개발 앱의 serviceGroup/계정 수명주기/허용 IPC/설정 화면을 LocalVault에 연결. 생성·비밀번호/OS 잠금 해제·수동/화면잠금/렌더러 종료 보호·재진입 구현. 자동 회귀 및 실제 Electron 네이티브 smoke 통과. 실제 앱 OS 인증·재실행 확인 대기, 기존 todo DB 전환은 아직 하지 않는다. [검증](../reports/2026-09-09-e2ee-workspace-integration.md).

최근 진행: 2026-09-09 14:04 (KST). [구현 및 검증 결과·남은 구현·사용자 확인 구분](../reports/2026-09-09-e2ee-implementation-progress.md). 사용자 검증 생략을 완료로 간주하지 않는다.

업데이트: 2026-09-09 02:06 (KST)

구현·검증·남은 범위: [E2EE 구현 체크포인트](../reports/2026-09-08-e2ee-checkpoint.md). 사용자 실검증은 요청으로 생략하지만 자동 테스트·배포 신뢰·아키텍처 호환성 미완료 항목을 DONE으로 간주하지 않는다.

진행 승인: 사용자 요청으로 #45 사용자 설치 검증을 기다리지 않고 개발한다. 사용자 검증 생략은 검증 통과나 운영 데이터 전환 승인으로 해석하지 않는다.

선행: #45, #47

설계: [E2EE 패치](../patches/patch-20260907-e2ee.md)

범위: SQLCipher 등 드라이버 평가 후 encrypted replica/LDK, 복구 키 확인, OS 잠금·유휴 잠금, 렌더러 평문 정리. synthetic 먼저 검증. 실제 OS 잠금/재인증 검증에서 사용자 대기.

완료 조건: DB/WAL/outbox/search 암호화와 잠금·재시작·복구 검증.

## Windows 배포 아키텍처 — 2026-09-08 18:43 (KST)

사용자가 x64 전용 전환을 승인했다. Windows NSIS 대상을 x64로 변경하되 appId·제품명·사용자 데이터 경로를 유지한다. 테스트 설치본만 생성하며 실제 앱 덮어쓰기·기존 DB 전환은 자동 수행하지 않는다. #45 사용자 설치 검증 생략 요청은 유지하고 미검증으로 기록한다.

## x64 패키지 검증 — 2026-09-08 18:50 (KST)

- x64 NSIS 빌드 성공: apps/desktop/dist/task48-x64/Thread Setup 1.1.3.exe, 151,202,086 bytes, Authenticode NotSigned. 검증용이며 운영 배포하지 않았다.
- SHA-256: EE1CCCD589677E851AB74F685DB836E148C6859449EB4FA11116723CCDF8D112
- 실제 x64 패키지에서 기존 appId/name, 실행 의존성, SQLite, 암호화 DB/WAL 평문 잔존 검사 및 재열기 통과.
- 이전 ia32 패키지가 만든 synthetic SQLite를 x64 패키지로 열어 schema version·기존 pending 예시를 보존하고 추가 쓰기 성공. 실제 NSIS 업그레이드/사용자 DB 검증과 구분한다.
- 보안 회귀 12개 재통과. build/electron/e2ee 모듈 5개가 최신 public 소스와 해시 일치.
- 실제 앱 설치·서버 변경·사용자 DB 읽기·암호화 전환·삭제는 수행하지 않았다. 제품 버전 1.1.3 유지. 기존 앱 통합·LDK 영속화·재인증 연결은 계속 미완료이므로 WIP 유지.

## 보호된 LDK 파일 영속화 — 2026-09-08 19:02 (KST)

- LocalVault가 environment/accountId/vaultId의 canonical hash 디렉터리에 OS 보호된 ldk.protected 및 암호화 DB를 보관한다. 파일·디렉터리는 새로 만들 때 exclusive 처리하여 기존 보관함을 덮어쓰지 않는다.
- 초기화 성공 marker는 키 파일 fsync 및 DB 스키마 생성·close 이후 마지막에 기록한다. 중단/키 분실/손상 시 원본을 유지하고 VAULT_RECOVERY_REQUIRED로 중단한다. 키 자동 재생성·원본 삭제·자동 복구는 하지 않는다.
- 생성·재열기, 키 분실 시 DB 바이트 보존, 불완전 초기화 거부, 계정/환경/보관함 격리 및 키 파일 바꿔치기 거부, OS 보호 unavailable 테스트 5개 통과. 전체 synthetic 보안 테스트 17개 통과.
- 실제 Electron 43.6.0 Windows x64의 safeStorage/DPAPI로 키 파일 영속화·재열기 및 암호화 DB 평문 미노출 smoke 통과. 테스트 전용 임시 폴더만 사용했다.
- 이번 모듈은 기존 로그인/Sync v2에 아직 연결하지 않았다. app runtime 사용자는 VaultSession 재인증 후에만 open해야 한다. 기존 설치본은 재빌드하지 않았다.

## 잠금 해제 구현 — 2026-09-08 20:09 (KST)

사용자 검증 결과(2026-09-09): Windows OS 인증 VERIFIED 및 취소 실패 처리 모두 정상 확인. 이는 OS 인증 fixture 검증이며 실제 앱의 보관함 잠금/동기화 검증과는 구분한다. 다음으로 인증과 VaultSession/DB 및 powerMonitor를 연결하는 coordinator를 구현한다. #48은 WIP 유지.

사용자가 Windows Hello/Touch ID 우선 + 별도 보관함 비밀번호 대체를 승인했다. Thread 자체 PIN은 추가하지 않는다. 두 방법을 화면에 표시하며 인증 취소 후 다른 방법을 자동 시도하지 않는다.

- 비밀번호 생성 최소 12자. worker Argon2id(64 MiB, 3회) + XChaCha20-Poly1305 및 scope AAD로 LDK를 보호한다. OS/비밀번호 양쪽으로 같은 암호화 DB를 연다. 비밀번호 자체는 파일에 저장하지 않으며 손상/오입력 시 원본을 초기화하지 않는다. JS 문자열 완전 소거나 같은 OS 사용자 권한의 악성 프로세스 차단을 보장하지 않는다.
- Windows HWND 기반 UserConsentVerifier helper 및 macOS Touch ID adapter 추가. helper 컴파일·beforePack 복사 hook 통과, 현재 PC availability=available. Windows 빌드에는 VS C++ 도구와 C++/WinRT Windows SDK가 필요하다.
- VaultUnlock은 방법 선택·로그인 비밀번호와의 차이 안내·확인 입력·취소/실패 처리를 제공한다. 아직 로그인/홈/Sync v2에 연결하지 않은 컴포넌트다. vault/ IPC 인수 로깅 차단만 추가했으며 전체 로그 감사 #56은 미완료다.
- React 56개, Electron Node-mode 비밀번호/DB 테스트 2개 통과. 후자에서 crashpad 접근 거부 경고가 있었지만 테스트 종료 코드는 0이었다. 새 설치본은 만들지 않았다.
- 사용자 검증 대기: apps/desktop에서 `pnpm exec electron tests/osAuth.manual.cjs` 실행. 버튼으로 인증 성공(VERIFIED)과 취소(성공으로 처리되지 않음)를 각각 확인한다. 별도 임시 프로필만 사용하고 실제 계정/DB/env를 읽지 않는다. 테스트 임시 프로필은 남을 수 있다.
- 실제 앱 연결·OS 잠금·복구/변경 UI·macOS 실기기 검증은 미완료. #48 WIP 유지. #57 이전 운영 마이그레이션/평문 삭제는 하지 않는다.
- production React 빌드 성공(ESLint 경고 있음). 첫 시도는 sandbox의 E: build 쓰기 EPERM으로 실패했고 승인된 권한으로 재실행해 통과했다. 설치 패키징 및 사용자 인증 성공/취소는 별도 검증이다.

## 인증과 DB 잠금 연결 — 2026-09-09 02:06 (KST)

사용자 검증 확인(2026-09-09 09:52 KST 기록): Win+L 후 화면 정리, DB 읽기 거부, 재인증 후 복원 모두 정상. 해당 시나리오는 재검증 요청하지 않는다. 절전/5분 유휴 실기기 확인과 기존 앱 통합은 미완료로 구분한다.

검증: coordinator 5개 포함 선택 보안 회귀 총 20개 통과. 수동 fixture main/preload Node 문법 검사 통과. 실제 수동 창의 화면/OS 이벤트 확인은 사용자 대기.

- VaultController가 OS 인증/비밀번호를 명시적으로 선택해 VaultSession에 연결한다. 키/DB 핸들은 메인에 남으며 자동 fallback은 없다. 화면 잠금·절전·기본 유휴 5분에서 DB close 및 renderer 정리 callback을 실행한다.
- 인증 중 잠금, 비밀번호 DB 열기 중 dispose, 중복 요청은 늦은 성공으로 다시 열리지 않는다. dispose 시 powerMonitor listener/timer를 해제한다.
- 실제 Electron 43.6.0 x64 synthetic DPAPI/암호화 DB smoke에서 coordinator unlock/read/lock/suspend 및 잠금 후 DB 거부 통과. 인증은 이 smoke에서 stub이며 실제 OS 인증 성공/취소는 사용자 확인 결과와 구분한다.
- 새로운 사용자 게이트: `apps/desktop`에서 `pnpm exec electron tests/vaultLock.manual.cjs`. 임시 테스트 보관함만 생성한다. 비밀번호는 공개 fixture 문자열 `fixture password`이며 실제 비밀번호를 입력하지 않는다. OS 인증 또는 테스트 비밀번호로 열어 문구 표시 → Win+L 후 복귀 시 문구 제거/DB 읽기 거부 → 다시 열기 확인. 절전 복귀/유휴 5분도 동일해야 한다.
- 테스트 창은 실제 홈/로그인/Sync v2 통합이 아니다. 원래 사용자 프로필과 DB는 읽지 않는다. 임시 fixture 디렉터리는 남겨두며 운영 마이그레이션/설치본 재빌드는 하지 않았다. 실제 화면 잠금·절전 검증 전 WIP 유지.
