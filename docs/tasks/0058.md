# Task 58: RMS Bullseye 저장소 만료 빌드 복구

생성: 2026-09-08 13:04 (KST)

상태: 🟢 DONE

완료: 2026-09-08 13:08 (KST)

사용자 로그: bullseye-security/InRelease expired, apt-get update exit 100. #44 로그인 검증용 서버 빌드가 차단되었다.

만료 검사를 비활성화하지 않고 지원되는 Node/Debian 베이스로 전환한다. 실제 DB·env와 컨테이너는 변경하지 않는다.

변경: node:16-bullseye-slim → node:22-bookworm-slim. pnpm 8.15.9 및 기존 lockfile 유지. Dockerfile 전용 ignore로 빌드 컨텍스트를 RMS 런타임 소스로 제한하고 실제 env·개발 의존성·다른 앱과 임시 데이터는 제외한다.

검증: 새 이미지를 별도 태그 thread-rms:bookworm-check로 빌드 성공. Node v22.23.2에서 apt update, diskusage 컴파일 및 빌드 도구 제거 후 native 실행 성공. 네트워크 없는 일회용 컨테이너에서 releaseAlerts.test.js와 chunkUpload.test.js 통과. express/mysql2/connect-busboy 로드 및 이미지의 .env/.env.production 부재 확인.

실제 MySQL 연결을 통한 서버 기동과 원격 호스트 시계 검증은 수행하지 않았다. 사용자 호스트의 시계가 앞서 있거나 프록시가 오래된 메타데이터를 제공하는 문제는 별도로 남을 수 있다. #44의 사용자 앱 검증 대기 상태는 유지한다.

반영: 변경된 작업 폴더에서 `docker compose up -d --build`. 서버를 별도로 배포한다면 해당 Dockerfile 변경을 먼저 반영해야 한다. 볼륨 삭제는 필요 없다.

근거: [Debian 11 LTS 종료 공지](https://www.debian.org/News/2026/20260831), [Node.js 지원 일정](https://github.com/nodejs/Release), [공식 Node Docker 이미지](https://github.com/nodejs/docker-node).
