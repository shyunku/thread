# Use 10.0.2.2 instead of localhost when the API runs on the Android emulator host.
APP_SERVER_ENDPOINT=http://localhost:4033
GOOGLE_OAUTH_WEB_CLIENT_ID=replace-with-google-web-client-id
GOOGLE_OAUTH_IOS_CLIENT_ID=replace-with-google-ios-client-id
